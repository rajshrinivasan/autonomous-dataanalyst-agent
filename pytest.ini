[pytest]
asyncio_mode = auto
testpaths = tests evals
markers =
    eval: end-to-end LLM eval tests — run with: pytest evals/ -m eval -v
    e2e: Playwright end-to-end tests — run with: pytest tests/e2e/ --run-e2e -v
