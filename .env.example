# OpenAI API key — get one at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Model to use for agents (gpt-4o-mini is cheaper; gpt-4o is more capable)
OPENAI_MODEL=gpt-4o-mini

# FastAPI server
HOST=127.0.0.1
PORT=8000

# Paths
DB_PATH=data/sample.db
CHARTS_DIR=output/charts
